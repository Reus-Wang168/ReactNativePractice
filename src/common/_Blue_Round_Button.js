import React, {Component} from 'react'

import glamorous from 'glamorous-native'

const BlueRoundButton = glamorous.view({
    width: 220,
    height: 30,
    margin:10,
    backgroundColor: '#206bf0',
    borderRadius: 20,
    justifyContent:'center',
    alignItems:'center'
});

export default BlueRoundButton;