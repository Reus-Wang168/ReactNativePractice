import React, {Component} from 'react'

import glamorous from 'glamorous-native'

const Button = glamorous.view({
    height: 30,
    margin: 10,
    padding: 10,
    backgroundColor: '#f8c802',
    borderRadius: 20,
    justifyContent: 'center',
    alignItems: 'center'
});

export default Button;